#+TITLE: Emacs External Packages
#+PROPERTY: header-args :results silent

* Overview

* Keybindings
- =C-s-j= :: Jump to a charactor within the visible buffer.

* Avy
#+begin_src elisp
  (use-package avy
    :ensure t
    :bind (("C-s-j" . avy-goto-char)))
#+end_src

* Eat
#+begin_src elisp :results silent
  (use-package eat
    :ensure t
    :config
    (setq eat-kill-buffer-on-exit t)
    (add-hook 'eat-mode-hook
              (lambda ()
                (buffer-face-set '(:family "Fira Mono" :height 150)))))
#+end_src
